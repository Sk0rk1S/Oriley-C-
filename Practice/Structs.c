#include <stdio.h>
#include <string.h>

struct adress{
    char = country;
    char = province;
    char = city;
    int = housenum[192];
};

void printadress(struct address ad);

int main() {

struct adress ad1;
struct adress ad2;


strxpy(ad1.street,"east x avenue");
}
