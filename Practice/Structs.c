#include <stdio.h>
#include <string.h>
//The char [] determines the maximum amount of characters outputted
struct adress{
    char = country[20];
    char = province[30];
    char = city[30];
    int = housenum[20];
};

void printadress(struct address ad);

int main() {

struct adress ad1;
struct adress ad2;


strxpy(ad1.street,"east x avenue");
}
